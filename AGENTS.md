# 《雾港来信》Codex 工作入口

本文件是 Codex 在本仓库执行任务时的总入口。岗位文件只规定工作方式，研究文件只提供建议；两者都不是剧情事实源。

## 1. 权威顺序

处理剧情与设计任务时，必须按以下优先级读取。当前开发目标已经切换到 R16.2；旧 v6 文件只作历史追溯：

1. `docs/CURRENT_TRUTH.md`（R16.2 当前真相摘要）
2. `docs/r16_2/source/雾港来信_R16.2_核心玩法与选择兑现版_全篇互动剧本.docx`（R16.2 正文）
3. `docs/r16_2/source/雾港来信_R16.2_程序接入表.xlsx`（R16.2 状态、路由与运行规格）
4. `docs/DECISIONS.md`
5. `docs/r16_2/legacy/` 与根目录 v6 文件仅用于历史追溯，不得覆盖 R16.2
6. 工作簿、CSV 与代码只能说明当前实现状态，不能自动推翻作者层设定

- v5 已作废，不得从 v5 恢复设定。
- 旧 v6 已降级为 legacy；不得把旧 v6 的人物、前史、世界事实、场次或商业口径混入 R16.2。
- R16.2 正文与程序接入表发生冲突时，先停止受影响的正式修改，报告冲突并等待确认；不得静默拼接两个版本。
- 如果两边都需要修改，必须报告冲突、列出各自影响并等待确认，不允许静默覆盖。
- 权威文件缺失、彼此冲突或无法可靠读取时，停止受影响的正式修改并标记“待确认”。不得用实现数据补写缺失的作者层文件。

## 2. 岗位自动编排

- 用户不需要主动指定岗位。GPT 必须根据任务范围自动选择并编排 `roles/` 中的岗位；岗位文件只规定工作方式，不构成剧情事实。
- 可用岗位为：`narrative`、`continuity`、`dialogue`、`playtest`、`commercial_reviewer`、`programmer`、`qa`。
- 核心剧情、核心机制或 Demo 重大改动，默认至少经过 `narrative`、`continuity`、`playtest` 与 `commercial_reviewer` 四个岗位检查；涉及实现与验证时再加入 `programmer` 和 `qa`，涉及对白时加入 `dialogue`。
- 涉及推理、互动或信息释放时，必须优先读取竞品知识库：`research/COMPETITOR_INDEX.md`、`research/DESIGN_PATTERNS.md`、`research/COMMERCIAL_STRATEGY.md`。研究结论只能作为建议，不能覆盖权威剧情或已确认决策。
- 岗位检查发现需要修改正式剧情、人物、机制或商业策略时，必须回到本文件的正式修改闭环；未经用户明确确认，不得直接落地。

## 3. 禁止脑补

不得把下列内容自动升级为正式事实：

- 推测
- 类型惯例
- 历史常识
- NPC 证词
- 旧版本残留
- 当前代码里的旧实现
- 聊天中的未确认提案
- 岗位文件或研究文档中的建议

遇到未定内容，统一标记“待确认”。

## 4. 正式修改闭环

任何影响正式剧情、人物、机制或商业策略的修改必须遵循：

```text
发现问题
→ 读取权威文件
→ 提出修改
→ 用户明确确认
→ 更新 DECISIONS.md
→ 必要时同步 CURRENT_TRUTH.md
→ 再修改运行数据 / 代码
→ 运行检查
```

没有用户明确确认，不得把讨论稿、聊天结论、竞品建议或实现便利写成正式设定。任务必须明确目标、允许修改范围、禁止范围与验收方式；超出范围时停止并报告。

## 5. Codex 的职责

Codex 负责：

- 修改本地仓库
- 数据迁移
- Godot / GDScript
- 自动校验与测试
- 文件同步
- 资源导入工具
- 按明确 Task 执行并报告验证结果

Codex 不负责擅自：

- 重写人物动机
- 新增世界观
- 新增核心机制
- 新增第七个主角动词
- 为了让测试通过而修改剧情事实

## 6. 文件角色

- `roles/`：工作规则，不得成为剧情事实源。
- `research/`：研究、设计与商业建议，不得覆盖正式剧情或已确认决策。
- `content/程序生成_请勿手改/`：运行生成物，禁止手工修改。
- `docs/r16_2/` 中纳入仓库的 R16.2 DOCX/XLSX 是本轮作者与程序接入入口；运行时 JSON 只能由确定性编译器生成。
- 旧 v6 工作簿、CSV 与代码只说明历史实现状态；不得自动推翻 R16.2 作者层设定。
- v5、备份文件和旧测试：只用于历史追溯或回滚，不得恢复其剧情口径。

## 7. 最低交付要求

每次交付必须列出修改文件、依据、未解决冲突、验证命令与结果。失败测试不得隐藏；过期测试与真实实现缺陷必须分开报告。

## 8. 总调度与知识复用

- 主 GPT 是用户的唯一调度入口：负责任务分级、选择岗位、分配文件所有权、合并重复问题、汇总待确认项、组织验证并给出一份最终交付。用户无需自行指定岗位。
- 非平凡的跨岗位、跨文件、实现加验证或可能触及正式内容的任务，使用仓库技能 `mist-harbor-orchestrator`；详细路由和状态流见 `docs/AI_TEAM_OPERATING_SYSTEM.md`。
- 多代理只用于可独立分解的工作流。每个写入文件在同一时间只有一个 owner；评审岗默认只读；最终合并、冲突升级与用户确认由主 GPT 负责。
- 开始新调研前先查 `research/KNOWLEDGE_INDEX.md`。复用时必须检查状态、适用范围、有效基准、复用条件与刷新触发器；研究中的“已验证”不等于“本项目已采用”。
- 任务包、研究记录与岗位报告模板位于 `docs/templates/`。小型且低风险的快速任务可不生成持久任务包；涉及正式修改的任务不得跳过第 4 节的用户确认门。
- 可复用文档、调度技能或元数据结构变更后，运行 `python3 tools/validate_workflow_metadata.py --root .`。该工具只检查结构、路径和越权标记，不判断剧情是否正确。

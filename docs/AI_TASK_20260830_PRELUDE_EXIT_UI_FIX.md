# AI 任务包｜序章工作桌完成门槛与旧宅占位修复

> 文档性质：执行范围与调度记录，不是剧情事实源、机制事实源或商业结论。

## 基本信息

- 任务 ID：`TASK-20260830-PRELUDE-EXIT-UI-FIX`
- 用户问题：四个线索均已完成但没有进入下一段；场景仍显示旅行箱、父亲书桌和沈家旧宅。
- 执行档位：`standard`
- 当前状态：`CLOSED`
- 唯一目标：让四个现行工作桌热点完成后出现“去春和戏院”并进入 `前序·证据收束`；彻底移除本界面的旧宅、旅行箱和父亲文案。
- 非目标：不修改剧情正文、人物关系、采访函内容、信息释放或其他章节。

## 依据与结论

- `docs/雾港来信_序章与第一章完整剧情信息树_v6.md`：S01 是赵敬文的报馆工作桌；场景包含靠窗空椅、桌牌、旧报、采访函留底、工作文件夹、地图和采访本。
- 当前数据：四个现行热点分别写入“已查看旧采访本”“已查看画满铅笔圈的旧报”“已查看赵敬文采访函留底”“已查看记者证与采访本”。
- 当前缺陷：P0013 错查“已查看退稿原稿”，且只列三项；`scripts/main.gd` 仍硬编码“沈家旧宅、旅行箱、父亲书桌”。
- 权威冲突：无。本任务只修复现行实现对 v6 的偏离，不触发新的正式剧情决策。

## 岗位与 owner

- `programmer`：主 Codex；负责工作簿门槛、Godot UI 与测试实现。
- `continuity`：只读；确认只使用 v6 已有报馆物件，不引入新设定。
- `playtest`：只读；验收 0/4 → 3/4 → 4/4 与下一段出口。
- `qa`：独立验收视角；验证四项状态、旧词残留、跳转和回归。

## 允许修改

- `scripts/main.gd`
- `tests/test_prelude_free_exploration.gd`
- `tests/test_workbook_sync.gd`
- 当前两份作者工作簿中的 P0013
- `content/程序生成_请勿手改/剧情剧本.csv`（仅由同步器生成）
- `docs/CURRENT_TRUTH.md`
- 本任务包

## 明确禁止

- 不恢复 v5 的旅行箱、父亲遗物、沈家旧宅或父子关系。
- 不修改程序生成 CSV 以外的生成物，不手工编辑 CSV。
- 不修改采访函、死亡报道、草图内容或后续场次。

## 验收

- 初始显示四个现行工作桌热点，进度 0/4。
- 完成前三项时显示 3/4，不能提前离开。
- 完成第四项后显示 4/4，并出现“去春和戏院”。
- 点击后进入 `前序·证据收束`。
- 场景文本不含“沈家旧宅、旅行箱、父亲、父子合照”。
- 工作簿同步保持 357 行且两份作者入口字节一致。

## 回滚与阻塞

- 工作簿修改前记录两份 SHA-256 并保存临时副本；文本修改使用精确补丁回退。
- 若四个现行热点与权威剧情树冲突，停止工作簿写入；当前未发现冲突。

## 交付记录

- P0013 已改为同时检查四条现行查看记录，互动备注同步为“仅在案件线索 4/4 后出现”。
- Godot 场景已替换旧宅占位：`靠窗空椅与桌牌`、`赵敬文的工作桌`、`文件柜、地图与旧报`。
- 调试热点和必查项已同步为四个现行工作桌热点，不再引用旅行箱、父亲遗物或父子合照。
- 两份作者工作簿同步后的 SHA-256 均为 `162317c4923b0d5a075b31f8cda054f3bb36cc72aea754bcb56ec80462ce9d6a`；同步器生成运行数据 357 行。
- 针对性测试通过：`test_workbook_sync.gd`、`test_prelude_free_exploration.gd`、`test_reply_search_flow.gd`、`test_story_content.gd`、`test_secret_release_content.gd`、`test_s00_opening.gd`、`test_v343_story_content.gd`、两项赵敬文口径测试。
- 全量 GDScript 回归 16 项中 11 项通过；其余 5 项为仍依赖已废弃 v5 旅行箱/父亲信件流程或旧行数的过期测试：`test_chapter_switching.gd`、`test_choice_backtracking.gd`、`test_full_flow.gd`、`test_opening_demo_logic.gd`、`test_v27_story_content.gd`。未为通过这些测试而恢复旧设定。
- Python 口径测试、13 项工作流元数据测试及 `tools/validate_workflow_metadata.py --root .` 均通过。
- 未解决权威冲突：无。

# R16.2 存档（已撤销，只作历史追溯）

> **本目录不是剧情事实源。** R16.2 已按 `DEC-20260922-CANON-REVERT-V6` 撤销，当前正典是 v6。本目录完整保留 R16.2 期间的全部资料，用于追溯这段弯路和从中取用机制，不得作为剧情、人物或世界事实的依据。

## 为什么撤销

R16.2 正文首页「迁移边界」自述：旧 R15.2 / v6 / R16 正文原文件未继续挂载，本文件因此按已知角色约束、U01–U32、D65、U28/U32 规则重建。《R16.2 修改前后对照记录》首段同样声明不能冒充逐句 diff。也就是说，R16.2 是在看不到旧正文的情况下，仅凭人名清单与节点编号规格重写的**另一个故事**，不是 v6 的修订版。仓库内不存在 R15.2 与 R16 的任何文件，R16.2 自称的继承链不成立。

## 作废清单（不得混入 v6）

民国二十三年（1934）的时间设定；《雾港晨报》；陈守义；玉棠为陈九生侄女；阿成为方仲山之子；周明远出场；全篇无人死亡；U01–U32 事件序列；E01–E04 结局路由；以及依赖上述任何一项的节点编号、状态名与条件表达式。

## 保留清单（只保留机制，须改喂 v6 数据）

报纸生命周期三层分离；D15-B 五卡排序所代表的「输入 → 玩家整理 → 下一层输入」结构；U27 单题核稿；剪报册；来源许可与 off-record 承诺状态机；条件化台词写法；`tools/r16_2_compile.py` 的确定性编译管线与故事快照／全局探索隔离规则。完整表述见 `docs/CURRENT_TRUTH.md` 的「R16.2 保留资产」一节。以上均为「允许复用」，不等于「已经采用」。

## 目录内容

| 目录 | 内容 |
| --- | --- |
| `source/` | 交接包中原样纳入仓库的 R16.2 作者源文件（DOCX / XLSX） |
| `handoff/` | R16.2 当时的执行说明、迁移计划与验收计划 |
| `archived/` | 被 R16.2 替换掉、现已恢复的 `CURRENT_TRUTH` 与 `README` 的 R16.2 版本 |

`docs/r16_2/legacy/` 已删除：其中的两份 v6 文档与 `docs/` 根目录下的正本字节一致，重复保存会造成正典漂移；`CURRENT_TRUTH_v6_legacy.md` 与 `README_v6_legacy.md` 已恢复为 `docs/CURRENT_TRUTH.md` 与 `README.md`。历史版本可在 git 中按 `baseline-20260919` 追溯。

## 代码与数据现状

`scenes/r16_2_main.tscn`、`scripts/r16_2_*.gd`、`content/程序生成_请勿手改/r16_2_runtime.json` 与 `tests/test_r16_2_*.gd` 均不删除，保留为可运行的实现参考。`project.godot` 的 `run/main_scene` 已改回 v6 的 `res://scenes/main.tscn`。`baseline-20260919` 保留为回滚点。

# AI 任务包｜采访函后的回函搜寻

> 文档性质：执行范围与调度记录，不是剧情事实源、机制事实源或商业结论。

## 1. 基本信息

- 任务 ID：`TASK-20260830-REPLY-SEARCH`
- 任务标题：删除独立“回函缺失”入口，改为读信后主动寻找
- 创建时间：2026-08-30
- 调度员：主 Codex
- 用户原始命令：删除当前“回函缺失”页；点击采访函直接展示正文，读完后点击“寻找回信”，播放短动画并显示“没找到”。
- 执行档位：`formal`
- 当前状态：`CLOSED`

## 2. 唯一目标

- 要达成的结果：P0005—P0008 形成“点击采访函留底 → 阅读原文 → 点击寻找回信 → 搜寻动效 → 没找到 → 返回房间”的连续交互。
- 完成定义：独立“回函缺失”入口与旧占位文案消失；采访函原文保持不变；搜寻期间不可重复输入；结束后只把“报馆留底旁未见回函”写入玩家知识；作者工作簿、生成 CSV、运行界面和测试一致。
- 非目标：不判断林怀安是否写过或寄出回信；不修改人物动机、二十年前真相、后续林怀安证词、六个主角动词或其他序章热点。

## 3. 权威与输入

### 权威依据

| 路径 / 决策 ID | 权威范围 | 本任务如何使用 | 读取状态 |
|---|---|---|---|
| `docs/CURRENT_TRUTH.md` | 当前锁定事实与实现摘要 | 约束玩家知识，不把“未见”升级为“不存在” | 已读取 |
| `docs/剧情修订稿_v6_人物与前史.md` | 人物、前史与作者层事实 | 保持人物关系和私有知识不变 | 已读取 |
| `docs/雾港来信_序章与第一章完整剧情信息树_v6.md` | 序章实际场次与采访函原文 | 保留原文与“旁边没有回函”的可观察事实 | 已读取 |
| `docs/DECISIONS.md` | 已确认变更 | 追加本次项目负责人明确确认 | 已读取 |

### 实现与研究输入

| 路径 / 记录 ID | 类型 | 仅说明什么 | 是否可能过期 |
|---|---|---|---|
| 当前作者工作簿 P0005—P0008 | 实现现状 | 旧实现把“未见回函”和“读函”拆成两个热点 | 否 |
| `scripts/main.gd` | 实现现状 | 线索查看层当前只有“返回房间” | 否 |
| `PATTERN-20260830-10/11` | 研究建议 | 复用职业动作并避免主控替玩家连接线索 | 否 |
| `research/COMMERCIAL_STRATEGY.md` | 研究建议 | 输入—反馈链应清楚，Demo 不宜只点“继续” | 待验证 |

- 当前权威冲突：无。剧情树写明“旁边没有回函”，与用户要求的“寻找后没找到”一致。

## 4. 正式修改审批门

- 是否影响正式剧情、人物、机制或商业策略：`是`（改变序章信息释放顺序和局部交互）
- 拟改变内容：删除先看“回函缺失”再打开信件的两级热点；改为先读信，再由玩家主动搜寻。
- 影响范围：P0005—P0008、依赖旧中间记录的 P0013、决策/当前实现摘要、作者工作簿、生成 CSV、Godot 线索查看层、相关测试。
- 用户明确确认原文或决策 ID：本任务开头所引用户命令。
- 确认时间：2026-08-30
- `DECISIONS.md` 更新责任人：主 Codex
- `CURRENT_TRUTH.md` 是否需要同步：`是`
- 审批状态：`已确认`

## 5. 岗位与工作分解

- `narrative`：启用；检查动作链和信息释放。
- `continuity`：启用；检查“没找到”不越界为“没有回信”。
- `dialogue`：不启用；采访函原文不改，结果采用用户指定短语。
- `playtest`：启用；验收输入—动画—结果连续反馈。
- `commercial_reviewer`：启用；只检查 Demo 主动性与节奏，不改变商业策略。
- `programmer`：启用；实现表格驱动跳转和非循环 Tween。
- `qa`：启用；按稳定 ID、运行界面和负向路径验收。

## 6. 文件范围与唯一 owner

### 允许修改

| 文件 / 工作簿 | 唯一 owner | 允许的改动 | 合并 / 审查人 |
|---|---|---|---|
| `docs/DECISIONS.md`、`docs/CURRENT_TRUTH.md`、`docs/雾港来信_序章与第一章完整剧情信息树_v6.md` | 主 Codex | 记录已确认交互、场次顺序与知识边界 | continuity |
| 两份当前作者工作簿 | 主 Codex | 修改 P0005—P0008 与其直接依赖 P0013，并保持镜像一致 | narrative / qa |
| `scripts/main.gd` | 主 Codex | 通用线索页动作按钮与数据标签驱动搜寻动效 | programmer / qa |
| `tests/test_workbook_sync.gd`、`tests/test_reply_search_flow.gd` | 主 Codex | 数据和运行流程回归 | qa |
| `content/程序生成_请勿手改/剧情剧本.csv` | 同步器 | 仅由工作簿重新生成 | qa |

### 明确禁止修改

- 不手工编辑 `content/程序生成_请勿手改/`。
- 不修改采访函原文、后续林怀安证词、人物设定、其他场次或美术资产。
- 临时测试输出只允许写入 `user://` 或 `/tmp`，结束后清理。

### 回滚点

- 修改前记录 Git diff、两份工作簿 SHA-256；文本用精确补丁回退，工作簿用修改前临时副本恢复。

## 7. 研究复用计划

- 已搜索：`research/KNOWLEDGE_INDEX.md`、`COMPETITOR_INDEX.md`、`DESIGN_PATTERNS.md`、`COMMERCIAL_STRATEGY.md`。
- 直接复用：同一职业动作承载叙事反馈；材料不替玩家下结论；输入—反馈关系清楚。
- 是否需要新研究：`否`。

## 8. 验收计划

| 验收项 | 方法 / 命令 | 预期结果 | 验收人 | 结果 |
|---|---|---|---|---|
| 作者源与生成数据 | `test_workbook_sync.gd` | P0005—P0008 连续且旧文案消失 | qa | 通过 |
| 运行交互 | `test_reply_search_flow.gd` | 读信后仅见“寻找回信”，动画后见“没找到” | qa | 通过 |
| 内容/链接校验 | 相关 Godot 测试与同步器 | 357 行、无悬空下一话题 | qa | 通过 |
| Demo 人工检查 | 重启当前 Godot Demo | 可按用户描述操作 | playtest | 通过 |

- 必测正常路径：点击采访函留底，读信，寻找回信，等待动效，查看“没找到”，返回房间。
- 必测负向 / 边界路径：信页不出现旧“返回房间”；动画中无重复按钮；未点寻找前不写入“未见回函”；旧“回函缺失/无回函附件”不再出现。
- 独立 QA 是否必须：`是`（由 QA 视角单独执行验收命令并分类既存过期测试）。

## 9. 停止与阻塞条件

- 必须暂停写入：权威资料发生冲突、工作簿作者入口不唯一、生成链无法可靠运行，或实现必须新增剧情事实。
- 当前阻塞：无。

## 10. 交付记录

- 实际修改文件：`docs/DECISIONS.md`、`docs/CURRENT_TRUTH.md`、`docs/雾港来信_序章与第一章完整剧情信息树_v6.md`、本任务包、两份当前作者工作簿、同步生成的 `剧情剧本.csv`、`scripts/main.gd`、`tests/test_workbook_sync.gd`、`tests/test_reply_search_flow.gd`。
- 各岗位报告：narrative 确认采访函原文与既有因果不变；continuity 确认“没找到”仅限报馆档案范围；playtest 确认输入—动画—结果连续且动画期间无重复按钮；commercial_reviewer 确认主动动作比独立缺失页更清楚，不改变商业策略；programmer 使用表格演出标签实现通用动作按钮与非循环 Tween；qa 覆盖未点击、动画中、动画后与旧文案残留。
- 合并后的结论：用户确认的连续交互已完整同步；两份工作簿字节一致，生成表仍为 357 行。
- 未解决冲突：无。
- 待确认事项：无。
- 验证命令与结果：同步器通过；`test_workbook_sync.gd`、`test_reply_search_flow.gd`、`test_story_content.gd`、`test_secret_release_content.gd`、两项赵敬文口径测试、`test_s00_opening.gd`、`test_v343_story_content.gd` 全部通过；工作流元数据校验通过。
- 失败分类：`test_chapter_switching.gd`、`test_choice_backtracking.gd`、`test_full_flow.gd`、`test_opening_demo_logic.gd`、`test_prelude_free_exploration.gd`、`test_v27_story_content.gd` 仍依赖旅行箱、书信夹、父亲手写信或 367 行旧数据，分类为过期测试；未恢复 v5 内容。
- 最终状态：`CLOSED`。

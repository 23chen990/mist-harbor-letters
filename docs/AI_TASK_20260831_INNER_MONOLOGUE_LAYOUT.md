# AI 任务包｜内心独白改用下屏对话框

> 文档性质：执行范围与调度记录，不是剧情事实源、机制事实源或商业结论。

## 1. 基本信息

- 任务 ID：`AI-TASK-20260831-INNER-MONOLOGUE-LAYOUT`
- 用户原始命令：`内心独白也应该展示下屏的对话框，而不是这种大屏的`
- 执行档位：`standard`
- 当前状态：`VERIFIED`

## 2. 目标与边界

- 唯一目标：内心独白使用标准下屏对话框，不再覆盖场景中部。
- 完成定义：P0016 显示 `Rect2(70, 344, 1140, 325)` 下屏框，不再显示 `Rect2(80, 92, 1120, 520)` 大屏；身份、当前句和继续按钮保留，不强制显示立绘。
- 非目标：不修改工作簿、生成 CSV、独白文字、逐句顺序、知识状态或函件选项。
- 权威依据：用户明确确认；`DEC-20260831-32`。

## 3. 岗位与文件 owner

- `programmer`：主 Codex；唯一写入 owner，修改 `scripts/main.gd`。
- `playtest`：只读检查实际画面中场景可见面积、文字可读性和点击反馈。
- `qa`：通过独立测试文件及序章流程回归验证，不修改实现。
- 允许修改：`scripts/main.gd`、相关测试、`README.md`、`docs/DECISIONS.md`、`docs/CURRENT_TRUTH.md`、本任务包。
- 禁止修改：工作簿、程序生成 CSV、剧情台词与正式人物内容。

## 4. 验收与状态

| 验收项 | 方法 | 当前结果 |
|---|---|---|
| 布局缺陷复现 | `test_inner_monologue_layout.gd` | RED：下屏框缺失，中央大屏仍存在 |
| 定向回归 | 布局测试、序章过渡与末句选项测试 | 通过 |
| 实际画面 | 1280×720 渲染检查 | 通过：下屏框可读、背景可见、无人物立绘 |
| 全量回归 | 全部 Godot / Python / workflow 测试 | Godot 16 项通过；6 项既有旧章节/待确认迁移失败；Python、工作流与项目加载通过 |

- 当前阻塞：无。
- 范围外待确认：v6 工作簿完整迁移仍见 `AI_TASK_20260831_V6_WORKBOOK_SYNC.md`。
- 最终结论：仅移除内心独白大屏布局特例；台词、流程、知识状态、末句选项和工作簿均未改变。

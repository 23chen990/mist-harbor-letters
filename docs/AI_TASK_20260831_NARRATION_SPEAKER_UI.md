# AI 任务包｜场景动作背景化、对话框只显示人物发言

> 文档性质：执行范围与调度记录，不是剧情事实源、机制事实源或商业结论。

## 1. 基本信息

- 任务 ID：`AI-TASK-20260831-NARRATION-SPEAKER-UI`
- 任务标题：用背景 UI 表现场景动作，对话框只显示人物发言
- 创建时间：2026-08-31
- 调度员：主 Codex
- 用户原始命令：`这样我还以为是编辑说的话呢`；随后明确：`场景直接用ui背景表现出来，不要这样在对话框，对话框就只展示对话`
- 执行档位：`standard`
- 当前状态：`VERIFIED`
- 目标版本 / 分支 / 提交：当前本地工作区

## 2. 唯一目标

- 要达成的结果：场景动作不进入对话框，改由背景 UI 表现；对话框只显示真正说出口的人物台词和对应立绘。
- 完成定义：P0001 只形成 10 个人物对话拍；开场直接显示编辑第一句，下一次点击直接显示沈砚舟第一句，背景分别表现版样与推稿动作。
- 非目标：不修改剧情事实、对白内容、工作簿或生成 CSV；不处理仍待确认的 v6 工作簿完整迁移。

## 3. 权威与输入

- 权威依据：`docs/DECISIONS.md` 的 DEC-20260831-28（人物台词逐句主动点击）、DEC-20260831-29（退稿以稿件 UI 直接表达）和 DEC-20260831-31（场景动作背景化、对话框只显示人物发言）。
- 实现输入：当前 P0001 已使用“动作行 + 引号台词”的剧本格式；旧运行器把整行 `人物=编辑` 套给所有拆分节拍。
- 当前权威冲突：无。DEC-20260831-31 已明确取代上一轮“场景”对话拍方案，不改变工作簿文本与信息释放。

## 4. 正式修改审批门

- 是否影响正式剧情、人物、机制或商业策略：只调整已确认的 Demo UI 呈现规则；不改剧情、人物或核心机制。
- 用户明确确认原文：`我们都说了，每一句话进入下一句需要玩家主动点击`；`场景直接用ui背景表现出来，不要这样在对话框，对话框就只展示对话`。
- `DECISIONS.md` 更新责任人：主 Codex，已新增 DEC-20260831-31。
- `CURRENT_TRUTH.md` 是否需要同步：是，已同步。
- 审批状态：已确认。

## 5. 岗位与工作分解

- `programmer`：主 owner；实现通用剧本节拍解析和背景动作 UI。
- `playtest`：检查对话框纯度、一次点击一句、动作 / 台词顺序和背景反馈。
- `qa`：用独立测试文件验证用户可见结果，并回归开场、退稿 UI 和序章流程。
- 其他岗位不启用：本任务不改剧情、人物关系、对白或商业策略。

## 6. 文件范围与唯一 owner

- 允许修改：`scripts/main.gd`、三个相关测试、`README.md`、`docs/DECISIONS.md`、`docs/CURRENT_TRUTH.md` 与本任务包；唯一 owner 为主 Codex。
- 只读：权威文档、当前工作簿和 `content/程序生成_请勿手改/剧情剧本.csv`。
- 明确禁止修改：剧情事实、对白文本、工作簿和程序生成 CSV。
- 回滚点：仓库当前未建立 Git 跟踪；按本任务文件清单反向撤销。

## 7. 验收计划与结果

| 验收项 | 方法 | 结果 |
|---|---|---|
| 对话框与背景职责 | `test_narration_speaker_ui.gd` | 通过：动作不生成对话拍；对话框仅有人物发言；背景存在动作 UI |
| 主动逐句推进 | `test_manual_line_advance.gd` | 通过：P0001 为 10 个人物对话拍，一次点击一句 |
| 退稿 UI | `test_rejected_manuscript_ui.gd` | 通过：退稿稿件 UI 保持存在并在离场后清理 |
| 序章回归 | `test_s00_opening.gd` | 通过：开场信息与后续自由探索可达 |
| 实际画面 | Godot 1280×720 渲染抓取 | 通过：编辑首句 / 沈砚舟首句只显示人物对话，背景分别显示版样与推稿动势，无动作原文 |
| 全量 Godot 回归 | 顺序执行 `tests/*.gd` | 15 项通过；6 项为既有旧章节口径或待确认的 v6 迁移测试失败，与本改动基线一致 |
| 工作流与项目加载 | Python 元数据测试、校验器、Godot 启动 | 全部通过 |

## 8. 停止与阻塞条件

- 当前阻塞：无。
- 范围外待确认：v6 工作簿完整迁移仍受函件“原件先不动”分支缺少正式闭环文本阻塞，记录在 `AI_TASK_20260831_V6_WORKBOOK_SYNC.md`。

## 9. 交付记录

- 合并结论：上一轮“场景”标签仍让动作占据对话框，不符合最终要求；现已把动作解析成台词携带的背景 UI 状态，对话框只保留人物发言。
- 未解决冲突：本任务内无。
- 失败分类：本任务初始失败均为预期的缺陷复现或与新规则冲突的过期断言；修改后定向回归全部通过。全量剩余失败为 5 个旧章节测试和 1 个仍待确认的 v6 工作簿迁移测试，不是本轮回归。
- 最终状态：`VERIFIED`
